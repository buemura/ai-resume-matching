services:
  db:
    image: pgvector/pgvector:pg16
    restart: unless-stopped
    env_file: .env
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

  # api:
  #   build: ./apps/api
  #   restart: unless-stopped
  #   env_file: .env
  #   ports:
  #     - "8000:8000"
  #   depends_on:
  #     - db
  #   volumes:
  #     - ./apps/api:/app

  # web:
  #   build: ./apps/web
  #   restart: unless-stopped
  #   ports:
  #     - "5173:5173"
  #   depends_on:
  #     - api
  #   volumes:
  #     - ./apps/web:/app
  #     - /app/node_modules

volumes:
  pgdata:
